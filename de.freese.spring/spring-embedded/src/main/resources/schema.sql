CREATE TABLE MY_TABLE (ID BIGINT PRIMARY KEY NOT NULL, NAME VARCHAR(250) NOT NULL, DATUM TIMESTAMP NOT NULL)
create sequence test_seq as bigint start with 1 increment by 1

--CREATE SEQUENCE SEQU
--CREATE TABLE company ( id bigint GENERATED BY DEFAULT AS SEQUENCE SEQU PRIMARY KEY, name varchar(128) NOT NULL CHECK (name <> '') );
--GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY

CREATE TABLE KUNDE (PK BIGINT PRIMARY KEY NOT NULL, NACHNAME VARCHAR(255) NOT NULL, VORNAME VARCHAR(255))