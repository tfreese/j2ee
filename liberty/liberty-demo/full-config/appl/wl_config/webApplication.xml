<?xml version="1.0" encoding="UTF-8"?>
<server description="webApp definition">

    <!-- Shared libraries; move global libraries from lib/global to somewhere else -->
    <!-- <library id="applicationLib"> -->
    <!--     <fileset dir="${server_home}/appl/appl_libs" includes="*.*" excludes="h2*.jar" /> -->
    <!--     <folder dir="${server_home}/appl/wl_config" /> -->
    <!-- </library> -->

    <!-- War file application which uses shared library. Deployment directory like appl_monitor must be created by project. -->
    <!-- <webApplication -->
    <!--         id="liberty-demo" -->
    <!--         name="liberty-demo" -->
    <!--         location="${server-home}/appl/${deploy_folder}/liberty-demo.war" -->
    <!--         contextRoot="config"> -->
    <!--     <classloader commonLibraryRef="applicationLib, h2Driver" /> -->
    <!-- </webApplication> -->
    <webApplication location="liberty-demo.war" contextRoot="${context.root}" />

    <mpOpenAPI docPath="${context.root}/openapi" uiPath="${context.root}/openapi/ui" />

    <logging isoDateFormat="true" />

</server>
