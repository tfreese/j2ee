<?xml version="1.0" encoding="UTF-8"?>
<server description="Demo for REST-Services">

    <featureManager>
        <!-- <feature>jakartaee-10.0</feature> -->
        <feature>restfulWS-3.1</feature>
        <feature>jsonb-3.0</feature>
        <feature>enterpriseBeans-4.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint" host="localhost" httpPort="${http.port}" httpsPort="${https.port}" />

    <webApplication location="liberty-rest.war" contextRoot="${context.root}" />

    <!-- <managedExecutorService jndiName="concurrent/executor"> -->
    <!--     <concurrencyPolicy max="3" maxQueueSize="100" /> -->
    <!-- </managedExecutorService> -->
    <!-- <managedScheduledExecutorService jndiName="concurrent/scheduledExecutorService"> -->
    <!--     <concurrencyPolicy max="3" maxQueueSize="100" /> -->
    <!-- </managedScheduledExecutorService> -->

    <executor coreThreads="4" maxThreads="5" />
</server>
